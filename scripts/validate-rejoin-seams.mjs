import assert from 'node:assert/strict';
import { compileRejoinSeams, STANDARD_REJOIN_FAMILIES } from '../src/seams/rejoin.mjs';
const observedAt = '2026-09-10T18:25:00-06:00';
const transportFamilies = new Set(['A2A', 'MCP', 'CRM_MAIL', 'CLOUDFLARE']);
const peerFamilies = new Set(['A2A', 'MCP']);
function currentSeam(family, extra = {}) {
  const row = { seam_id: `${family.toLowerCase()}:primary`, family, required: true, state: 'CURRENT', target_ref: `${family.toLowerCase()}:target`, provider_family: family === 'ACK' ? 'XIIO' : 'GENERIC', capability_profile_ref: `${family.toLowerCase()}:capability:v1`, subject_generation: 'g2', current_generation: 'g2', evidence_ref: `${family.toLowerCase()}:evidence:g2`, readback_ref: `${family.toLowerCase()}:readback:g2`, observed_at: observedAt };
  if (transportFamilies.has(family)) row.endpoint_ref = `${family.toLowerCase()}:endpoint:g2`;
  if (peerFamilies.has(family)) Object.assign(row, { principal_ref: `${family.toLowerCase()}:principal:g2`, assignment_ref: `${family.toLowerCase()}:assignment:g2`, assignment_receipt_ref: `${family.toLowerCase()}:assignment-receipt:g2`, authority_receipt_ref: `${family.toLowerCase()}:authority-receipt:g2`, tool_binding_ref: `${family.toLowerCase()}:tool-binding:g2`, operation_profile_ref: `${family.toLowerCase()}:operation-profile:g2`, resource_ref: `${family.toLowerCase()}:resource:g2`, path_binding_ref: `${family.toLowerCase()}:path-binding:g2`, binding_root_ref: 'root:test', source_digest_ref: `sha256:${'a'.repeat(64)}`, binding_generation_ref: 'g2', binding_readback_ref: 'binding:readback:g2', requested_operation_class: 'READ', bound_operation_class: 'READ', expected_effect_class: 'READ_ONLY', observed_effect_class: 'READ_ONLY' });
  if (family === 'CRM_MAIL') Object.assign(row, { principal_ref: 'principal:crm-mail:g2', mailbox_address: 'agent@r1-lane.xi-io.com', authority_receipt_ref: 'crm-mail:authority-receipt:g2', provider_occurrence_ref: 'provider:mail:occurrence:g2', provider_occurrence_generation_ref: 'g2' });
  if (family === 'CLOUDFLARE') Object.assign(row, { provider_occurrence_ref: 'provider:cloudflare:occurrence:g2', provider_occurrence_generation_ref: 'g2' });
  if (family === 'RETURN_CHAIN') Object.assign(row, { result_ref: 'result:g2', return_ref: 'return:g2', apply_return_ref: 'apply-return:g2' });
  if (family === 'DETONATOR') Object.assign(row, { detonator_ref: 'detonator:g2', trip_debt_ref: 'trip-debt:g2' });
  return { ...row, ...extra };
}
function allCurrent() { return STANDARD_REJOIN_FAMILIES.map((family) => currentSeam(family)); }
function compile(seams, rootExtra = {}) { return compileRejoinSeams({ root_ref: 'root:test', agent_ref: 'agent:test', subject_generation: 'g2', current_generation: 'g2', seams, ...rootExtra }); }
let result = compile(allCurrent());
assert.equal(result.status, 'CURRENT_BOUNDED'); assert.equal(result.current, true); assert.equal(result.standard_family_denominator, 17); assert.equal(result.current_required, 17);
const staleAck = allCurrent(); staleAck[0] = currentSeam('ACK', { subject_generation: 'g1' }); result = compile(staleAck); assert.equal(result.status, 'STALE'); assert.deepEqual(result.refresh_obligations.map((row) => row.family), ['ACK']);
const mcpIndex = STANDARD_REJOIN_FAMILIES.indexOf('MCP');
const providerWait = allCurrent(); providerWait[mcpIndex] = currentSeam('MCP', { state: 'UNKNOWN', evidence_ref: null, readback_ref: null, resolution_class: 'TRUE_WAIT', wake_when: 'provider://mcp/registration-readback' }); result = compile(providerWait); assert.equal(result.true_wait_refresh_count, 1); assert.equal(result.machine_resolvable_refresh_count, 0);
const malformedWait = allCurrent(); malformedWait[mcpIndex] = currentSeam('MCP', { state: 'UNKNOWN', evidence_ref: null, readback_ref: null, resolution_class: 'TRUE_WAIT', wake_when: null }); result = compile(malformedWait); assert.equal(result.machine_resolvable_refresh_count, 1); assert.ok(result.seams[mcpIndex].invalidators.includes('TRUE_WAIT_WITHOUT_WAKE'));
const peerIdentityMissing = allCurrent(); peerIdentityMissing[mcpIndex] = currentSeam('MCP', { principal_ref: null, assignment_ref: null, assignment_receipt_ref: null, authority_receipt_ref: null }); result = compile(peerIdentityMissing); assert.equal(result.status, 'UNKNOWN');
const guessedFunctionCall = allCurrent(); guessedFunctionCall[mcpIndex] = currentSeam('MCP', { tool_binding_ref: null, operation_profile_ref: null, resource_ref: null, path_binding_ref: null }); result = compile(guessedFunctionCall); assert.equal(result.status, 'UNKNOWN'); assert.ok(result.seams[mcpIndex].missing_bindings.includes('tool_binding_ref')); assert.ok(result.seams[mcpIndex].missing_bindings.includes('path_or_storage_binding_ref'));
const noToolBinding = allCurrent(); noToolBinding[mcpIndex] = currentSeam('MCP', { tool_binding_ref: null }); result = compile(noToolBinding); assert.equal(result.status, 'UNKNOWN');
const wrongOperation = allCurrent(); wrongOperation[mcpIndex] = currentSeam('MCP', { requested_operation_class: 'WRITE', bound_operation_class: 'READ', expected_effect_class: 'LOCAL_WRITE', observed_effect_class: 'LOCAL_WRITE' }); result = compile(wrongOperation); assert.equal(result.status, 'UNKNOWN'); assert.ok(result.seams[mcpIndex].invalidators.includes('OPERATION_CLASS_MISMATCH'));
const localWriteForProvider = allCurrent(); localWriteForProvider[mcpIndex] = currentSeam('MCP', { requested_operation_class: 'WRITE', bound_operation_class: 'WRITE', expected_effect_class: 'PROVIDER_WRITE', observed_effect_class: 'LOCAL_WRITE', effect_receipt_ref: 'effect:local:g2', provider_occurrence_ref: null }); result = compile(localWriteForProvider); assert.equal(result.status, 'UNKNOWN'); assert.ok(result.seams[mcpIndex].invalidators.includes('EFFECT_CLASS_MISMATCH'));
const fakeC010Status = allCurrent(); fakeC010Status[mcpIndex] = currentSeam('MCP', { requested_operation_class: 'WRITE', bound_operation_class: 'WRITE', expected_effect_class: 'PROVIDER_WRITE', observed_effect_class: 'PROVIDER_WRITE', effect_receipt_ref: null, provider_occurrence_ref: null }); result = compile(fakeC010Status); assert.equal(result.status, 'UNKNOWN'); assert.ok(result.seams[mcpIndex].missing_bindings.includes('provider_occurrence_ref'));
const wrongRoot = allCurrent(); wrongRoot[mcpIndex] = currentSeam('MCP', { binding_root_ref: 'root:other' }); result = compile(wrongRoot); assert.equal(result.status, 'UNKNOWN'); assert.ok(result.seams[mcpIndex].invalidators.includes('BINDING_ROOT_MISMATCH'));
const invalidDigest = allCurrent(); invalidDigest[mcpIndex] = currentSeam('MCP', { source_digest_ref: 'sha256:not-a-real-digest' }); result = compile(invalidDigest); assert.equal(result.status, 'UNKNOWN'); assert.ok(result.seams[mcpIndex].invalidators.includes('SOURCE_DIGEST_INVALID'));
const staleBinding = allCurrent(); staleBinding[mcpIndex] = currentSeam('MCP', { binding_generation_ref: 'g1' }); result = compile(staleBinding); assert.equal(result.status, 'UNKNOWN'); assert.ok(result.seams[mcpIndex].invalidators.includes('BINDING_GENERATION_MISMATCH'));
const staleProviderOccurrence = allCurrent(); staleProviderOccurrence[mcpIndex] = currentSeam('MCP', { requested_operation_class: 'WRITE', bound_operation_class: 'WRITE', expected_effect_class: 'PROVIDER_WRITE', observed_effect_class: 'PROVIDER_WRITE', effect_receipt_ref: 'effect:provider:g2', provider_occurrence_ref: 'provider:occurrence:g2', provider_occurrence_generation_ref: 'g1', effect_receipt_generation_ref: 'g2' }); result = compile(staleProviderOccurrence); assert.equal(result.status, 'UNKNOWN'); assert.ok(result.seams[mcpIndex].invalidators.includes('PROVIDER_OCCURRENCE_GENERATION_MISMATCH'));
const boundProviderEffect = allCurrent(); boundProviderEffect[mcpIndex] = currentSeam('MCP', { requested_operation_class: 'WRITE', bound_operation_class: 'WRITE', expected_effect_class: 'PROVIDER_WRITE', observed_effect_class: 'PROVIDER_WRITE', effect_receipt_ref: 'effect:provider:g2', provider_occurrence_ref: 'provider:occurrence:g2', provider_occurrence_generation_ref: 'g2', effect_receipt_generation_ref: 'g2' }); result = compile(boundProviderEffect); assert.equal(result.status, 'CURRENT_BOUNDED'); assert.equal(result.authority_granted, false); assert.equal(result.provider_effects, 0);
const crmIndex = STANDARD_REJOIN_FAMILIES.indexOf('CRM_MAIL'); const crmConfiguredOnly = allCurrent(); crmConfiguredOnly[crmIndex] = currentSeam('CRM_MAIL', { readback_ref: null }); result = compile(crmConfiguredOnly); assert.equal(result.status, 'UNKNOWN');
const crmIdentityMissing = allCurrent(); crmIdentityMissing[crmIndex] = currentSeam('CRM_MAIL', { principal_ref: null, mailbox_address: null, authority_receipt_ref: null, provider_occurrence_ref: null, provider_occurrence_generation_ref: null }); result = compile(crmIdentityMissing); assert.equal(result.status, 'UNKNOWN'); assert.ok(result.seams[crmIndex].missing_bindings.includes('provider_occurrence_ref'));
const cfIndex = STANDARD_REJOIN_FAMILIES.indexOf('CLOUDFLARE'); const cloudflareHostnameOnly = allCurrent(); cloudflareHostnameOnly[cfIndex] = currentSeam('CLOUDFLARE', { readback_ref: null, provider_occurrence_ref: null, provider_occurrence_generation_ref: null }); result = compile(cloudflareHostnameOnly); assert.equal(result.status, 'UNKNOWN');
const returnIndex = STANDARD_REJOIN_FAMILIES.indexOf('RETURN_CHAIN'); const returnWithoutApply = allCurrent(); returnWithoutApply[returnIndex] = currentSeam('RETURN_CHAIN', { apply_return_ref: null }); result = compile(returnWithoutApply); assert.equal(result.status, 'UNKNOWN');
const detIndex = STANDARD_REJOIN_FAMILIES.indexOf('DETONATOR'); const detonatorWithoutTripDebt = allCurrent(); detonatorWithoutTripDebt[detIndex] = currentSeam('DETONATOR', { trip_debt_ref: null }); result = compile(detonatorWithoutTripDebt); assert.equal(result.status, 'UNKNOWN');
const nA = allCurrent().filter((row) => row.family !== 'MCP'); nA.push({ seam_id: 'mcp:not-applicable', family: 'MCP', required: false, applicability: 'N_A_WITH_EVIDENCE', state: 'N_A_WITH_EVIDENCE', evidence_ref: 'evidence:profile-no-mcp' }); result = compile(nA); assert.equal(result.status, 'CURRENT_BOUNDED');
const missingFamily = allCurrent().filter((row) => row.family !== 'MCP'); result = compile(missingFamily); assert.deepEqual(result.missing_families, ['MCP']);
result = compile(allCurrent(), { subject_generation: 'g1' }); assert.equal(result.status, 'REBASE_REQUIRED');
assert.throws(() => compileRejoinSeams({ root_ref: 'root:test', agent_ref: 'agent:test', subject_generation: 'g2', current_generation: 'g2', seams: [] }), /SEAM_DENOMINATOR_REQUIRED/);
const duplicate = allCurrent(); duplicate.push(currentSeam('ACK')); assert.throws(() => compile(duplicate), /DUPLICATE_SEAM_ID/);
console.log('REJOIN_SEAMS_PASS current=17/17 call_binding_required=1 guessed_path_rejected=1 operation_scope_bound=1 local_effect_not_provider=1 fake_status_not_effect=1 root_binding_required=1 digest_shape_required=1 binding_generation_required=1 provider_occurrence_generation_required=1 peer_identity_access_required=1 crm_mail_provider_occurrence_required=1 cloudflare_provider_occurrence_required=1 stale_only_affected=1 provider_wait=1 return_apply_required=1 detonator_trip_debt_required=1');
