# Base44 Studio / Installer / Launcher donor — 2026-09-17

Status: EXPERIMENTAL DONOR ONLY. Not production-ready. No authority, provider-effect, billing, deployment, legal-effect, or runtime-readiness is implied.

## Provenance split — factual chronology only

User states that the original Base44 shell, launcher, desktop/projection-folder concept, Xibalba underground atmosphere, cog-background treatment, parallax/dust/rotation work, and related visual/interaction concepts were created by the user before separation/divorce.

Keep this separate from later work:
- `PRE-SEPARATION ORIGINAL DONOR`: original shell/launcher/desktop visual and interaction concepts, Xibalba atmosphere, cog brand treatment, and original Base44 concept work.
- `POST-SEPARATION DERIVATIVE / HARDENING`: 2026 glass-box hardening, SDK donor mapping, Ward/HEX qualification, current installer/launcher integration, cyan Ibal identity substitution, and current xi-io search mapping.

This file records chronology/provenance only. It does not determine legal ownership, family-property classification, valuation, authorship rights against third parties, or entitlement.

Source app: Base44 `xi-io Node` (`693dffcccab0bea12e1e7952`).
Pre-hardening checkpoint: `6aab7769c3ef330faf770f2c` / `385da9ddb2094440d81fef787edc7b9899647373`.
Post-hardening checkpoint: `6aab7b5a4e1c60b030e6e7c4` / `7c43367cc26094280ba8cb19894e056af7132096`.

## Harvest now

1. Installer / device uplink donor
   - pairing-code / device-link concepts
   - device verification / uplink flow
   - installer terminal concepts

2. Launcher / desktop-shell donor
   - OS-style desktop / launcher
   - projection folders and desktop objects
   - dock/start/window behavior
   - satellite pop-out/window mesh concepts
   - service-layer glass-box mapping

3. Brand environment — preserve as authored
   - cog / gear background is a BRAND STAPLE, not disposable donor decoration
   - Xibalba underground atmosphere: dust, parallax, breathing scale, rotation/tilt, glow/orb lighting, vignette/depth
   - `COG_BACKGROUND = BRAND_STAPLE`
   - `PARALLAX_DUST_ROTATION = PRESERVE_AS_AUTHORED_PRESENTATION`
   - old Xibalba logo role may be replaced by the cyan Ibal identity mark

4. Search mapping
   - Base44 search/launcher concept should map onto the existing xi-io search widget / command surface
   - do not invent a second search engine

5. Glass-box mapping seam
   - `src/components/design/API_CONTRACT.jsx`
   - `src/components/api/service.jsx`
   - experimental `getDesktopInstalledApps()` boundary added
   - `DesktopEnvironment` now consumes the service mapping instead of direct Base44 entity calls for installed desktop apps

## Deferred donor-only concepts

The following are explicitly captured but out of tonight's critical path:
- osFORGE / Dreamcatcher / custom Linux-kernel builder concepts
- video editor / media systems
- phone / VoIP concepts
- screen-share / WebRTC concepts
- satellite remote-control concepts beyond what launcher needs
- future shared-screen / device-link expansion
- broader full-stack roadmap / forge concepts

These remain `EXPERIMENTAL_DONOR` until separately graduated through the shared SDK preflight + Ward boundary + HEX hostiles.

## Hard boundaries

- `EXPERIMENTAL_DONOR != PRODUCTION_READY`
- `SOURCE_PRESENT != RUNTIME_QUALIFIED`
- `PROJECTION != AUTHORITY`
- `BASE44_PROVIDER_BINDING != XIIO_PROVIDER_BINDING`
- `VISUAL_BRAND_PRIMITIVE != FULL_SHELL_ADOPTION`
- `DONOR_BUILD_PASS != STUDIO/TAURI/ARIES_RUNTIME_PASS`
- `PRE_SEPARATION_ORIGINAL != POST_SEPARATION_HARDENING`

## Measured donor smoke

Base44 static donor smoke: PASS.
Base44 `npm run build`: PASS.
Build emitted only stale browser-data warnings; exit code 0.

## Tonight's HVT use

Use only the installer/launcher-relevant primitives above. Do not expand into FORGE or future communication/media features before the court-document/template deliverables are complete.
